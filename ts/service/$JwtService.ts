import * as jwt from "jsonwebtoken"
export class JwtService {
  createToken(payload: { account: string; }) {
    //const roles = this.reflector.get<string>('roles', context.getHandler());
    throw new Error("Method not implemented.");
  }

}